
#Require "src/drivers/USB/Descriptors.rlx"
#Require "src/drivers/USB/IDs.rlx"
#Require "src/drivers/USB/Device.rlx"


i8 PCI_INTERFACE_UHCI := 0

void* FirstUHCIController := null

namespace USB::UHCI {
    namespace RegisterFields {
        bits Command As i16 {
            [0] RunStop
            [1] HostControllerReset
            [2] GlobalReset
            [3] EnterGlobalSuspend
            [4] ForceGlobalResume
            [5] EnableSoftwareDebug
            [6] Configured
            [7] Enable64BitPackets
        }

        bits Status As i16 {
            [0] InterruptWasFromTransfer
            [1] InterruptWasFromError
            [2] ResumeDetected
            [3] HostSystemError
            [4] HostControllerError
            [5] HostControllerHalted
        }

        bits Interrupt As i16 {
            [0] InterruptOnTimerOrCRC
            [1] InterruptOnResume
            [2] InterruptOnComplete
            [3] InterruptOnShortPacket
        }

        bits Port As i16 {
            [0] DevicePresent
            [1] ConnectStatusChange
            [2] Enable
            [3] EnableChange
            [4:5] LineStatus
            [6] ResumeDetected
            [7] Present
            [8] LowSpeedDevice
            [9] Reset
            [12] Suspend
        }
    }

    enum Registers As i32 {
        Command := 0
        Status := 2
        InterruptEnable := 4
        FrameNumber := 6
        FrameListBase := 8
        StartOfFrameModify := 0xC
        Port1 := 0x10
        Port2 := 0x12
    }

    enum TransferToken {
        IN := 0x69,
        OUT := 0xE1,
        SETUP := 0x2D
    }

    struct TransferDescriptor {
        i32 Link

        bits As i32 {
            [0:10] ActualLength
            [17] BitstuffError
            [18] TimeoutOrCRCError
            [19] NAKReceived
            [20] BabbleDetected
            [21] DataBufferError
            [22] Stalled
            [23] Active
            [24] ShouldInterruptOnComplete
            [25] IsIsochronous
            [26] IsLowSpeedDevice
            [27:28] InterruptAfterErrorCount
            [29] ShortPacketDetect
        }

        bits As i32 {
            [21:31] MaximumLength
            [19] DataToggle
            [15:18] Endpoint
            [8:14] DeviceAddress
            TransferToken [0:7] Token
        }

        i32 BufferPointer

        void* DataBuffer
        i32 PeriodicFrameNumber
    }

    struct QueueHead {
        i32 HorizontalLink
        i32 VerticalLink

        i8* Name
        i8 Complete
        Synchronization::Semaphore* Completed
        QueueHead* Previous
        TransferHandle* Handle
    }

    struct PCIeOverlay {
        i8[0x20] _
        i32 IOBase
        ; 0x24
        i8[0x3C] _
        i8 USBVersion
    }

    enum LinkFlags As i32 {
        Terminator := 1,
        IsQueueHead := 2,
        DepthFirst := 4
    }

    enum QueueHeadPeriod {
        _1_MS := 0,
        _2_MS := 1,
        _4_MS := 2,
        _8_MS := 3,
        _16_MS := 4,
        _32_MS := 5,
        _64_MS := 6,
        _128_MS := 7,
        _256_MS := 8,
        _512_MS := 9,
        _1024_MS := 10
    }

    class TransferHandle implements USB::ITransferHandle, derives INew {
        IController* Controller

        SetupPacket* Setup
        QueueHead* QH
        PointerArray* TDs

        void* OriginalDataBuffer
        void* RelocatedDataBuffer

        define void New(IController* Controller, SetupPacket* Setup, QueueHead* QH, void* DataBuffer, void* RelocatedBuffer) {
            this->Controller := Controller

            this->Setup := Setup
            this->QH := QH
            this->TDs := new PointerArray()

            this->OriginalDataBuffer := DataBuffer
            this->RelocatedDataBuffer := RelocatedBuffer

            this->QH->Handle := this
        }

        define void AddTransferDescriptor(TransferDescriptor* TD) {
            this->TDs->Add(TD)
        }

        define void Complete() {
            this->QH->Complete := true

            if (this->Setup->DataDirection = RequestDataPhaseDirection::DeviceToHost) {
                void* Buffer := this->OriginalDataBuffer
                i32 Count := this->TDs->Count()

                for (i32 Index := 0, Index < Count, Index++) {
                    TransferDescriptor* TD := this->TDs->At(Index)

                    if (TD->Token != TransferToken::IN) {
                        continue
                    }

                    i32 Size := TD->ActualLength + 1

                    MoveMemory(Buffer, TD->DataBuffer, Size)

                    Buffer += Size
                }
            }

            this->QH->Completed->Signal()
        }

        define ITransferHandle* Wait() {
            this->QH->Completed->Wait()

            return this
        }

        define void Free() {
            this->Controller->Free(this)
            this->TDs->Free()
            this->Delete()
        }
    }

    enum PeriodicTransferType {
        Interrupt,
        Isochronous
    }

    class PeriodicTransferHandle implements USB::IPeriodicTransferHandle, derives INew {
        IController* Controller
        
        PeriodicTransferType Type
        i32 Period
        i32 Phase

        PointerArray* ScheduledTDs
        PointerArray* FreeTDs

        void* Buffer

        Synchronization::Semaphore* Fired

        define void New(IController* Controller, PeriodicTransferType Type, i32 Period, i32 Phase, void* Buffer) {
            this->Controller := Controller
            this->Type := Type
            this->Period := Period
            this->Buffer := Buffer

            this->ScheduledTDs := new PointerArray()
            this->FreeTDs := new PointerArray()

            this->Fired := new Synchronization::Semaphore(0)
        }

        define void Complete() {
            this->Fired->Signal()
        }

        define void* Wait() {
            this->Fired->Wait()

            return this->Buffer
        }

        define void Free() {

        }

        define void Stop() {
            
        }


    }


    

    class Controller implements USB::IController, derives INew {
        PCIeAddress* Address
        PCIeDevice* Device

        PalHeap* Heap

        void* HeapPoolVirtual
        i64 HeapPoolPhysical

        QueueHead*[11] PeriodicQueueHeads
        PointerArray* QueueHeadsInFlight

        PointerArray* PeriodicTransfers

        i32 IOBase
        i8 USBVersion

        i32* FrameList
        i64 PhysicalFrameList

        Synchronization::Semaphore* Interrupt

        i32 NextDeviceAddress

        define void New(PCIeAddress* Address, PCIeDevice* _Device) {
            this->Address := Address
            this->Device := _Device

            this->Interrupt := new Synchronization::Semaphore(0)

            FirstUHCIController := this

            PCIeOverlay* Overlay := _Device As void*

            this->IOBase := _Device->ReadBAR(4)
            this->USBVersion := Overlay->USBVersion

            Print("UCHI IOBase %x, Version %x\n", this->IOBase, this->USBVersion)

            this->QueueHeadsInFlight := new PointerArray()
            this->NextDeviceAddress := 1

            this->PeriodicTransfers := new PointerArray()
        }

        define void Write16(Registers Target, i16 Value) {
            OutW((this->IOBase + (Target As i32)) As i16, Value)
        }
        define void Write32(Registers Target, i32 Value) {
            OutL((this->IOBase + (Target As i32)) As i16, Value)
        }

        define i16 Read16(Registers Target) {
            return InW((this->IOBase + (Target As i32)) As i16)
        }

        define i32 Read32(Registers Target) {
            return InL((this->IOBase + (Target As i32)) As i16)
        }

        define i32 VirtualToPhysical(void* Virtual) {
            return ((Virtual As i64) - (this->HeapPoolVirtual As i64) + this->HeapPoolPhysical) As i32
        }
        define void* PhysicalToVirtual(i32 Physical) {
            return ((Physical As i64) - this->HeapPoolPhysical + (this->HeapPoolVirtual As i64)) As void*
        }

        define i32 PackLink(void* VirtualAddress, ... Flags) {
            i32 Packed := 0

            if (VirtualAddress != null) {
                Packed := this->VirtualToPhysical(VirtualAddress)
            }

            i32 FlagCount := Flags[0] As i32

            for (i32 Index := 0, Index < FlagCount, Index++) {
                Packed |= Flags[Index + 1] As i32
            }

            return Packed
        }

        define i32 RemoveLinkFlag(i32 Link, LinkFlags Flag) {
            return Link & ~(Flag As i32)
        }

        define i32 AddLinkFlag(i32 Link, LinkFlags Flag) {
            return Link | (Flag As i32)
        }

        define void Indent(i32 Count) {
            for (i32 Index := 0, Index < Count, Index++) {
                Info("  ")
            }
        }

        define void DumpLink(i32 Indent, i32 Link) {
            if (Link & 1) {
                Info("%x Terminator\n", Link)
            }
            else if (Link & 2) {
                QueueHead* Queue := this->PhysicalToVirtual(Link & 0x7FFF_FFF0)

                Info("%x %s QueueHead -> ", Link, Queue->Name)

                this->DumpLink(Indent, Queue->VerticalLink)

                this->Indent(Indent + 1)
                Info(" %c-> ", '^')
                this->DumpLink(Indent + 1, Queue->HorizontalLink)
            }
            else {
                TransferDescriptor* TD := this->PhysicalToVirtual(Link & 0x7FFF_FFF0)

                Info("%x TransferDescriptor {%s, %i, %x} -> ", Link, nameof:(TD->Token), TD->MaximumLength, TD->BufferPointer)

                this->DumpLink(Indent, TD->Link)
            }
        }
        define void DumpLink(i32 Link) {
            this->DumpLink(0, Link)
        }

        i8 Stopped

        define void RemovePeriodicTransfers(i32 Index) {
            for (i32 Period := 10, Period >= 0, Period--) {
                i32 Value := 1 << Period

                if (Index % Value = 0) {
                    this->FrameList[Index] := this->PackLink(this->PeriodicQueueHeads[Period], LinkFlags::IsQueueHead)
                    break
                }
            }
        }

        define i8 FrameWasInLast(i32 CurrentFrameNumber, i32 LastFrames, i32 FrameNumber) {
            i32 Difference := CurrentFrameNumber - FrameNumber

            if (Difference < 0) {
                Difference += 1024
            }

            return Difference < LastFrames
        }

        define i32 TimeSinceFrame(i32 CurrentFrameNumber, i32 FrameNumber) {
            i32 Difference := CurrentFrameNumber - FrameNumber

            if (Difference < 0) {
                Difference += 1024
            }

            return Difference
        }

        define i32 TimeUntilFrame(i32 CurrentFrameNumber, i32 FrameNumber) {
            i32 Difference := FrameNumber - CurrentFrameNumber

            if (Difference < 0) {
                Difference += 1024
            }

            return Difference
        }

        define void ScheduleNextQuarter(i32 CurrentFrameNumber, PeriodicTransferHandle* Handle) {
            i32 TimeUntilFinalScheduledFrame := 0

            i32 Count := Handle->ScheduledTDs->Count()

            ;Info("Before: %i Scheduled TDs, %i Free TDs\n", Count, Handle->FreeTDs->Count())

            for (i32 Index := Count - 1, Index >= 0, Index--) {
                TransferDescriptor* TD := Handle->ScheduledTDs->At(Index)

                if (TD->Active = 0 || this->FrameWasInLast(CurrentFrameNumber, 256, TD->PeriodicFrameNumber)) {
                    ; This frame is scheduled in the past (aka it's done and we can reuse the TD)

                    Handle->ScheduledTDs->Remove(Index)
                    Handle->FreeTDs->Push(TD)

                    this->RemovePeriodicTransfers(TD->PeriodicFrameNumber)
                }
                else {
                    i32 TimeUntil := this->TimeUntilFrame(CurrentFrameNumber, TD->PeriodicFrameNumber)

                    if (TimeUntil > TimeUntilFinalScheduledFrame) {
                        TimeUntilFinalScheduledFrame := TimeUntil
                    }
                }
            }

            ;Info("After: %i Scheduled TDs, %i Free TDs\n", Handle->ScheduledTDs->Count(), Handle->FreeTDs->Count())

            ;Info("%i frames until schedule runs out\n", TimeUntilFinalScheduledFrame)

            if (TimeUntilFinalScheduledFrame > 256) {
                ; The next quarter is already scheduled, no need to do anything

                return
            }

            i32 FinalScheduledFrame := CurrentFrameNumber + TimeUntilFinalScheduledFrame
            i32 MaxScheduledFrame := CurrentFrameNumber + 256

            if (FinalScheduledFrame % Handle->Period != Handle->Phase) {
                Info("FinalScheduledFrame %i is not aligned with Period %i\n", FinalScheduledFrame, Handle->Period)

                i32 Remainder := FinalScheduledFrame % Handle->Period

                FinalScheduledFrame += Handle->Period - Remainder
            }

            ;Info("Scheduling next quarter: %i to %i\n", FinalScheduledFrame, MaxScheduledFrame)

            i32 Count := Handle->FreeTDs->Count()

            for (i32 Index := Count - 1, Index >= 0, Index--) {
                TransferDescriptor* TD := Handle->FreeTDs->At(Index)

                TD->PeriodicFrameNumber := FinalScheduledFrame % 1024
                FinalScheduledFrame += Handle->Period

                TD->Link := this->FrameList[TD->PeriodicFrameNumber]
                this->FrameList[TD->PeriodicFrameNumber] := this->PackLink(TD)

                TD->Active := true

                ;Info("Scheduling TD %x for frame %i\n", TD, TD->PeriodicFrameNumber)

                Handle->FreeTDs->Remove(Index)
                Handle->ScheduledTDs->Push(TD)

                if (FinalScheduledFrame >= MaxScheduledFrame) {
                    break
                }
            }
        }

        define i32 GetCurrentFrameNumber() {
            return (this->Read16(Registers::FrameNumber) As i32) & 0x3FF
        }

        define void InterruptThread() {
            loop {
                i8 IsRealInterrupt := this->Interrupt->Wait(128)

                ;Info("UHCI Interrupt Thread woke up\n")

                i32 CurrentFrameNumber := this->GetCurrentFrameNumber()

                i32 Count := this->PeriodicTransfers->Count()

                for (i32 Index := 0, Index < Count, Index++) {
                    PeriodicTransferHandle* Handle := this->PeriodicTransfers->At(Index)

                    ; Check if any TDs scheduled for this handle have been recently completed

                    i32 ScheduledTDsCount := Handle->ScheduledTDs->Count()

                    for (i32 TDIndex := 0, TDIndex < ScheduledTDsCount, TDIndex++) {
                        TransferDescriptor* TD := Handle->ScheduledTDs->At(TDIndex)

                        ; If the TD is recent

                        if (this->FrameWasInLast(CurrentFrameNumber, 256, TD->PeriodicFrameNumber)) {
                            if !(TD->Active) {
                                ; If the TD was completed

                                Handle->Complete()
                            }
                        }
                    }

                    this->ScheduleNextQuarter(CurrentFrameNumber, Handle)
                }

                i32 Count := this->QueueHeadsInFlight->Count()

                for (i32 Index := Count - 1, Index >= 0, Index--) {
                    QueueHead* QH := this->QueueHeadsInFlight->At(Index)

                    ;Print("InterruptThread: IFQH #%i: VerticalLink %x, HorizontalLink %x\n", Index, QH->VerticalLink, QH->HorizontalLink)

                    if (QH->VerticalLink & 1) {
                        QH->Handle->Complete()

                        QH->Previous->VerticalLink := QH->HorizontalLink

                        if (QH->HorizontalLink & LinkFlags::IsQueueHead) {
                            QueueHead* NextQH := this->PhysicalToVirtual(QH->HorizontalLink & 0x7FFF_FFF0)
                            NextQH->Previous := QH->Previous
                        }

                        ;this->Stop := true

                        ;RegisterFields::Command CommandFields := 0 As RegisterFields::Command
                        ;this->Write16(Registers::Command, CommandFields As i16)

                        this->QueueHeadsInFlight->Remove(Index)
                    }
                }
            }
        }

        define void Stop() {
            this->Stopped := true

            RegisterFields::Command CommandFields := 0 As RegisterFields::Command
            this->Write16(Registers::Command, CommandFields As i16)

            Print("UHCI Controller stopped\n")
        }

        define void OnInterrupt() {
            ;Print("UHCI Interrupt!\n")

            ; Clear status bit to allow interrupt line to de-assert

            RegisterFields::Status StatusFields := this->Read16(Registers::Status) As RegisterFields::Status

            if (StatusFields.InterruptWasFromError) {
                Print("UHCI Error!\n")
                this->Stop()
            }
            else if (StatusFields.HostSystemError) {
                Print("UHCI Host System Error!\n")
                this->Stop()
            }
            else if (StatusFields.HostControllerError) {
                Print("UHCI Host Controller Error!\n")
                this->Stop()
            }

            if (StatusFields.InterruptWasFromTransfer) {
                this->Interrupt->Signal()

                ; Writing 1s clears, so rewriting the current value clears the register
                this->Write16(Registers::Status, StatusFields As i16)
            }

            LocalAPIC->EOI()
        }

        static void OnInterrupt(AMD64Context* Context, i32 InterruptNumber) {
            (FirstUHCIController As Controller*)->OnInterrupt()
        }

        define QueueHead* AllocateQueueHead(i8* Name) {
            QueueHead* Out := this->Heap->Allocate(#QueueHead)

            Out->HorizontalLink := this->PackLink(null, LinkFlags::Terminator)
            Out->VerticalLink := this->PackLink(null, LinkFlags::Terminator)
            Out->Name := Name

            return Out
        }

        define TransferDescriptor* AllocateTransferDescriptor(i32 Address, i32 Endpoint, TransferSpeed Speed, TransferToken Token, i32 Length, void* Buffer) {
            TransferDescriptor* Out := this->Heap->Allocate(#TransferDescriptor)

            Out->Link := this->PackLink(null, LinkFlags::Terminator)

            Out->InterruptAfterErrorCount := 3
            Out->Active := true

            Out->DeviceAddress := Address As i8
            Out->Endpoint := Endpoint As i8

            if (Speed = TransferSpeed::Low) {
                Out->IsLowSpeedDevice := true
                Out->MaximumLength := 7
            }
            else {
                Out->IsLowSpeedDevice := false
                Out->MaximumLength := 63
            }
            
            Out->ShouldInterruptOnComplete := true
            Out->Token := Token
            ;Out->ActualLength := Length As i16

            if (Buffer) {
                Out->BufferPointer := this->VirtualToPhysical(Buffer)
            }

            return Out
        }

        define void* Allocate(i32 Size) {
            return this->Heap->Allocate(Size)
        }

        define void ScheduleQueueHead(QueueHeadPeriod Period, QueueHead* QH) {
            i32 Index := Period As i32

            QueueHead* PeriodicQueue := this->PeriodicQueueHeads[Index]

            QH->Previous := PeriodicQueue

            i32 OldVerticalLink := PeriodicQueue->VerticalLink
            QH->HorizontalLink := OldVerticalLink

            if (OldVerticalLink & LinkFlags::IsQueueHead) {
                QueueHead* OldVerticalQH := this->PhysicalToVirtual(OldVerticalLink & 0x7FFF_FFF0)
                OldVerticalQH->Previous := QH
            }

            PeriodicQueue->VerticalLink := this->PackLink(QH, LinkFlags::IsQueueHead)
        }

        define ITransferHandle* SendControlTransfer(i32 Address, i32 Endpoint, TransferSpeed Speed, SetupPacket* Setup, void* Buffer, i32 DataLength) {
            QueueHead* QH := this->AllocateQueueHead("Transfer")
            QH->Completed := new Synchronization::Semaphore(0)

            SetupPacket* RelocatedSetupPacket := this->Heap->Allocate(#SetupPacket)
            MoveMemory(RelocatedSetupPacket, Setup, #SetupPacket)

            void* RelocatedBuffer := this->Heap->Allocate(DataLength)

            if (Setup->DataDirection = USB::RequestDataPhaseDirection::HostToDevice) {
                MoveMemory(RelocatedBuffer, Buffer, DataLength)
            }
            
            TransferHandle* Handle := new TransferHandle(this, RelocatedSetupPacket, QH, Buffer, RelocatedBuffer)

            TransferDescriptor* SetupTD := this->AllocateTransferDescriptor(Address, Endpoint, Speed, TransferToken::SETUP, 8, RelocatedSetupPacket)
            Handle->AddTransferDescriptor(SetupTD)

            TransferToken Token := TransferToken::OUT
            TransferToken StatusToken := TransferToken::IN

            if (Setup->DataDirection = USB::RequestDataPhaseDirection::DeviceToHost) {
                Token := TransferToken::IN
                StatusToken := TransferToken::OUT
            }

            i32 MaxSize := 8

            if (Speed = TransferSpeed::Full) {
                MaxSize := 64
            }

            TransferDescriptor* Previous := SetupTD
            void* PacketData := RelocatedBuffer

            while (DataLength > 0) {
                i32 PacketSize := MaxSize

                if (DataLength < MaxSize) {
                    PacketSize := DataLength
                }

                TransferDescriptor* TD := this->AllocateTransferDescriptor(Address, Endpoint, Speed, TransferToken::IN, PacketSize, PacketData)
                TD->DataBuffer := PacketData

                Handle->AddTransferDescriptor(TD)

                PacketData += PacketSize
                DataLength -= PacketSize
            }

            TransferDescriptor* StatusTD := this->AllocateTransferDescriptor(Address, Endpoint, Speed, StatusToken, 0, null)

            Handle->AddTransferDescriptor(StatusTD)

            i32 Count := Handle->TDs->Count()

            for (i32 Index := 0, Index < Count - 1, Index++) {
                TransferDescriptor* Previous := Handle->TDs->At(Index)
                TransferDescriptor* Next := Handle->TDs->At(Index + 1)

                Previous->Link := this->PackLink(Next, LinkFlags::DepthFirst)
            }

            QH->VerticalLink := this->PackLink(SetupTD, LinkFlags::DepthFirst)

            this->QueueHeadsInFlight->Push(QH)

            this->DumpLink(this->PackLink(QH, LinkFlags::IsQueueHead))
            this->DumpLink(this->PackLink(this->PeriodicQueueHeads[QueueHeadPeriod::_1_MS], LinkFlags::IsQueueHead))

            this->ScheduleQueueHead(QueueHeadPeriod::_1_MS, QH)

            return Handle
        }

        define IPeriodicTransferHandle* ScheduleInterruptTransfer(i32 Address, i32 Endpoint, TransferSpeed Speed, RequestDataPhaseDirection Direction, i32 Period) {
            TransferToken Token := TransferToken::OUT

            if (Direction = USB::RequestDataPhaseDirection::DeviceToHost) {
                Token := TransferToken::IN
            }

            i32 MaxSize := 8

            if (Speed = TransferSpeed::Full) {
                MaxSize := 64
            }

            void* Buffer := this->Heap->Allocate(MaxSize)
            PeriodicTransferHandle* Handle := new PeriodicTransferHandle(this, PeriodicTransferType::Interrupt, Period, 0, Buffer)

            i32 TDsPerQuarter := (256 / Period) + 1

            for (i32 Index := 0, Index < TDsPerQuarter, Index++) {
                TransferDescriptor* TD := this->AllocateTransferDescriptor(Address, Endpoint, Speed, Token, MaxSize, Buffer)

                Handle->FreeTDs->Push(TD)
            }
            
            this->PeriodicTransfers->Push(Handle)

            i32 CurrentFrameNumber := this->GetCurrentFrameNumber()

            this->ScheduleNextQuarter(CurrentFrameNumber, Handle)

            return Handle
        }

        define void Free(ITransferHandle* Handle) {
            TransferHandle* Transfer := Handle As TransferHandle*

            this->Heap->Free(Transfer->Setup)

            i32 Count := Transfer->TDs->Count()

            for (i32 Index := 0, Index < Count, Index++) {
                TransferDescriptor* TD := Transfer->TDs->At(Index)
                this->Heap->Free(TD)
            }

            this->Heap->Free(Transfer->QH)
            this->Heap->Free(Transfer->RelocatedDataBuffer)
        }

        define void WaitAndFree(ITransferHandle* Handle) {
            TransferHandle* Transfer := Handle As TransferHandle*

            Transfer->Wait()

            this->Free(Transfer)
        }

        define void CheckPort(Registers Port) {
            if (this->Stopped) {
                return
            }

            if (Port = Registers::Port1) {
                return
            }

            RegisterFields::Port PortFields := this->Read16(Port) As RegisterFields::Port

            Info("Port %x: %b (Present? %i)\n", Port, PortFields, PortFields.Present)

            if (PortFields.DevicePresent) {
                ;Print("Device present on port %d\n", Port)

                if (PortFields.ConnectStatusChange) {
                    Print("Connect status change on port %d\n", Port)

                    PortFields.ConnectStatusChange := false
                    this->Write16(Port, PortFields As i16)

                    PortFields := this->Read16(Port) As RegisterFields::Port
                    PortFields.Reset := true
                    this->Write16(Port, PortFields As i16)

                    SleepMS(100)
                    PortFields := this->Read16(Port) As RegisterFields::Port
                    PortFields.Reset := false
                    this->Write16(Port, PortFields As i16)

                    SleepMS(50)
                    PortFields := this->Read16(Port) As RegisterFields::Port
                    PortFields.Enable := true
                    this->Write16(Port, PortFields As i16)

                    loop {
                        PortFields := this->Read16(Port) As RegisterFields::Port

                        if (PortFields.Enable) {
                            Print("Port %x enabled\n", Port)
                            Info("Port %x enabled\n", Port)
                            break
                        }

                        SleepMS(100)
                    }

                    ;this->FrameList[200] := this->RemoveLinkFlag(this->FrameList[200], LinkFlags::Terminator)

                    i32 Address := this->NextDeviceAddress++

                    Print("Setting address %i\n", Address)

                    SetupPacket SetupSetAddress
                    SetupSetAddress.MakeSetAddress(Address As i8)

                    this->SendControlTransfer(0, 0, TransferSpeed::Low, &SetupSetAddress, null, 0)->Wait()->Free()

                    SleepMS(50)

                    ; Address now set, hand off to USB driver

                    USB::Device* Dev := new USB::Device(this, Address)
                    USB::DeviceDescriptor* _Device := Dev->Descriptor

                    /* SleepMS(10)

                    this->Stop := true
                    RegisterFields::Command CommandFields := 0 As RegisterFields::Command
                    this->Write16(Registers::Command, CommandFields As i16) */

                    Print("What a wonderful %s %s you have plugged in !!!!!!\n", 
                        USBIDs->GetVendorName(_Device->VendorID),
                        USBIDs->GetDeviceName(_Device->VendorID, _Device->ProductID)
                    )

                    Print("USB Device Strings:\n")
                    Print("  Manufacturer: %usb-string-descriptor\n", Dev->ManufacturerString)
                    Print("  Product: %usb-string-descriptor\n", Dev->ProductString)
                    Print("  Serial Number: %usb-string-descriptor\n", Dev->SerialNumberString)

                    ;Device* Dev := new Device(this, _Device)

                    SleepMS(10)

                    this->Stop()
                    
                }

                if (PortFields.EnableChange) {
                    Print("Enable change on port %d\n", Port)
                }
            } else {
                ;Print("No device present on port %d\n", Port)
            }
        }

        define void CheckPortsThread() {
            loop {
                this->CheckPort(Registers::Port1)
                this->CheckPort(Registers::Port2)

                SleepMS(1000)
            }
        }

        define void Setup() {

            i32 Pin := this->Device->InterruptPin - 1
            i32 PinToIOAPIC := ACPI_GetInterruptNumber(this->Address->Segment, this->Address->Bus, this->Address->Device, this->Address->Function, Pin)

            RegisterInterruptHandler(0x35, &self:OnInterrupt)

            FirstIOAPIC->ForwardInterrupt(PinToIOAPIC, LocalAPIC->ID, 0x35)
            FirstIOAPIC->Mask(PinToIOAPIC, false)

            Print("Pin %i, IOAPIC %i\n", Pin, PinToIOAPIC)

            ;Print("CanUseMsi? %i", this->Device->CanUseMSI())
            ;Halt()

            this->IOBase := this->Device->ReadBAR(4) & 0b1111_1111_1110_0000

            Info("UCHI IOBase %x, Version %x\n", this->IOBase, this->USBVersion)

            this->FrameList := KernelPageAllocator->AllocateVirtual(1)
            this->PhysicalFrameList := KernelAddressSpace->VirtualToPhysical(this->FrameList)

            i32 PoolPageCount := PAGES_IN_2MB / 8 ; 512k pool

            this->HeapPoolVirtual := Trampoline~>PageTree->FindGap(PoolPageCount, 0x4000_0000_0000, PoolPageCount)
            this->HeapPoolPhysical := PhysicalMemory->Allocate(PoolPageCount)

            Trampoline~>PageTree->SetRange(this->HeapPoolVirtual, this->HeapPoolPhysical, PoolPageCount, PAGE_PRESENT | PAGE_WRITE)

            this->Heap := KernelHeapAllocate('UHCIHeap', #PalHeap)
            this->Heap->Initialize(this->HeapPoolVirtual, PoolPageCount * PAGE_SIZE)
            this->Heap->Bootstrap()

            Print("UHCI Heap @ %x, Physical %x\n", this->HeapPoolVirtual, this->HeapPoolPhysical)

            for (i32 Index := 0, Index <= 10, Index++) {
                this->PeriodicQueueHeads[Index] := this->AllocateQueueHead(FormatString("Every %i MS", 1 << Index))
            }

            i32 TerminatorLink := this->PackLink(null, LinkFlags::Terminator)

            this->PeriodicQueueHeads[0]->HorizontalLink := this->PackLink(null, LinkFlags::Terminator)

            for (i32 Index := 0, Index < 10, Index++) {
                QueueHead* LowerQH := this->PeriodicQueueHeads[Index + 1]
                QueueHead* HigherQH := this->PeriodicQueueHeads[Index]

                LowerQH->HorizontalLink := this->PackLink(HigherQH, LinkFlags::IsQueueHead)

                ;Print("Set '%s' horizontal link to '%s'\n", LowerQH->Name, HigherQH->Name)

                ;this->DumpLink(this->PackLink(LowerQH, LinkFlags::IsQueueHead))
                ;this->DumpLink(LowerQH->HorizontalLink)
            }

            ;this->DumpLink(this->PackLink(this->PeriodicQueueHeads[3], LinkFlags::IsQueueHead))

            for (i32 Index := 0, Index < 1024, Index++) {
                for (i32 Period := 10, Period >= 0, Period--) {
                    i32 Value := 1 << Period

                    if (Index % Value = 0) {
                        this->FrameList[Index] := this->PackLink(this->PeriodicQueueHeads[Period], LinkFlags::IsQueueHead)
                        break
                    }
                }
            }

            /* QueueHead* GetDescriptorQH := this->AllocateQueueHead("Get Descriptor")

            SetupPacket* Setup := this->Allocate(#SetupPacket)
            Setup->MakeGetDescriptor(DescriptorType::Device, 0, 18)

            TransferDescriptor* SetupTD := this->AllocateTransferDescriptor(TransferSpeed::Low, TransferToken::SETUP, 8, Setup)

            this->DescriptorBuffer := this->Allocate(18)

            TransferDescriptor* In0 := this->AllocateTransferDescriptor(TransferSpeed::Low, TransferToken::IN, 8, this->DescriptorBuffer)
            TransferDescriptor* In1 := this->AllocateTransferDescriptor(TransferSpeed::Low, TransferToken::IN, 8, this->DescriptorBuffer + 8)
            TransferDescriptor* In2 := this->AllocateTransferDescriptor(TransferSpeed::Low, TransferToken::IN, 2, this->DescriptorBuffer + 16)

            ;GetDescriptorQH->Vertical.Pointer := this->VirtualToPhysical(SetupTD)
            ;GetDescriptorQH->Vertical.IsQueueHead := true

            GetDescriptorQH->VerticalLink := this->PackLink(SetupTD, LinkFlags::DepthFirst)
            GetDescriptorQH->HorizontalLink := this->PackLink(null, LinkFlags::Terminator)

            ;SetupTD->Next.Pointer := this->VirtualToPhysical(In0)
            SetupTD->Link := this->PackLink(In0, LinkFlags::DepthFirst)

            ;In0->Next.Pointer := this->VirtualToPhysical(In1)
            In0->Link := this->PackLink(In1, LinkFlags::DepthFirst)

            ;In1->Next.Pointer := this->VirtualToPhysical(In2)
            In1->Link := this->PackLink(In2, LinkFlags::DepthFirst)

            ;In2->Next.Terminator := true
            In2->Link := this->PackLink(null, LinkFlags::Terminator)

            ;this->FrameList[200] := this->PackLink(GetDescriptorQH, LinkFlags::IsQueueHead, LinkFlags::Terminator)

            this->PeriodicQueueHeads[QueueHeadPeriod::_1_MS]->VerticalLink := this->PackLink(GetDescriptorQH, LinkFlags::IsQueueHead, LinkFlags::Terminator)

            ;Print("FrameList[200]: %x\n", this->FrameList[200])

            ;this->DumpLink(this->FrameList[0])

            ;Halt()

            Print("GetDescriptorQH: %x\n", KernelAddressSpace->VirtualToPhysical(GetDescriptorQH))

            ;Print("Link0: %x\n", *(Link0 As i32*))



            Print("%hexdump\n", this->FrameList, 16) */

            this->Device->Write16(0xC0, 0x2000)
            this->Device->Command |= PCI_COMMAND_REGISTER_BUS_MASTER As i16

            RegisterFields::Command CommandFields := 0 As RegisterFields::Command
            CommandFields.GlobalReset := true
            CommandFields.HostControllerReset := true
            this->Write16(Registers::Command, CommandFields As i16)

            SleepMS(20)

            CommandFields := this->Read16(Registers::Command) As RegisterFields::Command

            if (CommandFields.HostControllerReset) {
                Print("Host controller reset failed\n")
                return
            }

            CommandFields.GlobalReset := false
            this->Write16(Registers::Command, CommandFields As i16)

            this->Write32(Registers::FrameListBase, this->PhysicalFrameList As i32)
            this->Write16(Registers::FrameNumber, 0)

            RegisterFields::Interrupt InterruptFields := 0 As RegisterFields::Interrupt
            InterruptFields.InterruptOnComplete := true
            this->Write16(Registers::InterruptEnable, InterruptFields As i16)

            CommandFields.RunStop := true
            CommandFields.Enable64BitPackets := true
            this->Write16(Registers::Command, CommandFields As i16)

            Task* CheckPorts := Scheduler->AddLightTask(null, "UHCI Port Checker")
            CheckPorts~>Context->CallMethod(this, &self.CheckPortsThread)
            CheckPorts->Unblock()

            Task* InterruptThread := Scheduler->AddLightTask(null, "UHCI Interrupt Handler")
            InterruptThread~>Context->CallMethod(this, &self.InterruptThread)
            InterruptThread->Unblock()
        }

        define void Poll() {
            
        }
    }
}