
declare void SchedulerSleep(i64)

define void SleepMS(i32 Milliseconds) {
	i64 End := TIME_UPTIME_MS + Milliseconds
	
	if (IsSchedulerActive()) {
		SchedulerSleep(Milliseconds)
	}
	else {
		while (TIME_UPTIME_MS < End) {}
	}
}
